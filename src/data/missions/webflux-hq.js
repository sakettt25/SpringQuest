export const WEBFLUX_HQ_MISSIONS = [
  { id:'wf-001', worldId:'webflux-hq', order:1, title:'Reactive @RestController', difficulty:'easy', xpReward:220, coinReward:45, estimatedTime:12,
    story:'Time to build your first reactive REST API! In WebFlux, controllers return Mono and Flux instead of plain objects.',
    objective:'Create a @RestController that returns Mono<String> from a @GetMapping endpoint.',
    starterCode:'// TODO: Add @RestController annotation\npublic class HelloController {\n\n    // TODO: Add @GetMapping("/hello")\n    // Return Mono<String> with "Hello WebFlux!"\n}',
    requirements:{ annotations:['RestController','GetMapping'], patterns:[{ name:'Mono return', pattern:'Mono<', required:true }] },
    tests:[ { name:'Returns Mono<String>', visible:true, validate:(c)=>({ passed:/Mono<String>/.test(c)&&/Mono\.just/.test(c), message:'Return type must be Mono<String>' }) } ],
    hiddenTests:[], hints:['Add @RestController above the class.','Return type: public Mono<String> hello()','return Mono.just("Hello WebFlux!");'],
    solution:'import org.springframework.web.bind.annotation.*;\nimport reactor.core.publisher.Mono;\n\n@RestController\npublic class HelloController {\n    @GetMapping("/hello")\n    public Mono<String> hello() {\n        return Mono.just("Hello WebFlux!");\n    }\n}',
    explanation:'In WebFlux, @RestController works the same as in Spring MVC, but you return reactive types. Mono<T> for single values, Flux<T> for streams. Spring automatically subscribes and serializes the result to the HTTP response.'
  },
  { id:'wf-002', worldId:'webflux-hq', order:2, title:'Flux Streaming Endpoint', difficulty:'easy', xpReward:230, coinReward:45, estimatedTime:12,
    story:'WebFlux can stream data to clients in real-time. Return a Flux and the client receives items as they are produced.',
    objective:'Create a @GetMapping that returns Flux<Product>. Also create an endpoint that streams Server-Sent Events using MediaType.TEXT_EVENT_STREAM_VALUE.',
    starterCode:'import org.springframework.web.bind.annotation.*;\nimport org.springframework.http.MediaType;\nimport reactor.core.publisher.Flux;\nimport java.time.Duration;\n\n// TODO: @RestController\npublic class ProductController {\n    // TODO: GET /products returning Flux<String>\n\n    // TODO: GET /products/stream with produces = TEXT_EVENT_STREAM_VALUE\n    //       Return Flux with delayElements for streaming effect\n}',
    requirements:{ annotations:['RestController','GetMapping'], patterns:[{ name:'Flux return', pattern:'Flux<', required:true },{ name:'SSE', pattern:'TEXT_EVENT_STREAM|text/event-stream', required:true }] },
    tests:[ { name:'Has SSE streaming endpoint', visible:true, validate:(c)=>({ passed:/TEXT_EVENT_STREAM|text\/event-stream/.test(c)&&/Flux</.test(c), message:'Use produces = TEXT_EVENT_STREAM_VALUE' }) } ],
    hiddenTests:[], hints:['@GetMapping(value="/stream", produces=MediaType.TEXT_EVENT_STREAM_VALUE)','Use Flux.interval(Duration.ofSeconds(1)) for timed streaming.','Each item is sent as a Server-Sent Event to the client.'],
    solution:'import org.springframework.web.bind.annotation.*;\nimport org.springframework.http.MediaType;\nimport reactor.core.publisher.Flux;\nimport java.time.Duration;\n\n@RestController\n@RequestMapping("/products")\npublic class ProductController {\n    @GetMapping\n    public Flux<String> getProducts() {\n        return Flux.just("Laptop","Phone","Tablet");\n    }\n\n    @GetMapping(value = "/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)\n    public Flux<String> streamProducts() {\n        return Flux.just("Laptop","Phone","Tablet","Watch","Headphones")\n            .delayElements(Duration.ofSeconds(1));\n    }\n}',
    explanation:'Server-Sent Events (SSE) enable real-time streaming from server to client. Set produces=TEXT_EVENT_STREAM_VALUE and return a Flux. Each emitted item becomes an SSE event. Use delayElements() to simulate real-time data. This is how live dashboards, notifications, and stock tickers work.'
  },
  { id:'wf-003', worldId:'webflux-hq', order:3, title:'Reactive @PostMapping', difficulty:'medium', xpReward:260, coinReward:55, estimatedTime:15,
    story:'Your manager says: "We need a POST endpoint that accepts a user and saves them reactively."',
    objective:'Create a @PostMapping that accepts Mono<User> as @RequestBody and returns Mono<User> with an assigned ID.',
    starterCode:'import org.springframework.web.bind.annotation.*;\nimport reactor.core.publisher.Mono;\n\n// TODO: @RestController @RequestMapping("/users")\npublic class UserController {\n    // User record/class with id, name, email\n\n    // TODO: POST /users accepting @RequestBody Mono<User>\n    // Assign an ID and return the saved user\n}',
    requirements:{ annotations:['RestController','PostMapping','RequestBody'], patterns:[{ name:'Mono<User>', pattern:'Mono<\\w*User', required:true }] },
    tests:[ { name:'POST with reactive body', visible:true, validate:(c)=>({ passed:/@PostMapping/.test(c)&&/@RequestBody/.test(c)&&/Mono</.test(c), message:'Use @PostMapping with @RequestBody Mono<User>' }) } ],
    hiddenTests:[], hints:['@PostMapping accepts @RequestBody Mono<User> userMono','Use .map() to transform: userMono.map(u -> new User(id, u.name, u.email))','Spring WebFlux deserializes the JSON body into Mono<User> lazily.'],
    solution:'import org.springframework.web.bind.annotation.*;\nimport reactor.core.publisher.Mono;\nimport java.util.concurrent.atomic.AtomicLong;\n\n@RestController\n@RequestMapping("/users")\npublic class UserController {\n    record User(Long id, String name, String email) {}\n    private final AtomicLong counter = new AtomicLong(1);\n\n    @PostMapping\n    public Mono<User> createUser(@RequestBody Mono<User> userMono) {\n        return userMono.map(u -> new User(counter.getAndIncrement(), u.name(), u.email()));\n    }\n}',
    explanation:'In WebFlux, @RequestBody can be Mono<T> — the body is deserialized reactively. Use .map() to transform the incoming data (e.g., assign an ID). This is non-blocking from HTTP parsing to response serialization. The Mono flows through the entire pipeline without blocking any thread.'
  },
  { id:'wf-004', worldId:'webflux-hq', order:4, title:'WebClient - Reactive HTTP Client', difficulty:'medium', xpReward:280, coinReward:60, estimatedTime:15,
    story:'RestTemplate is blocking. In reactive apps, use WebClient — the non-blocking HTTP client.',
    objective:'Use WebClient to make GET and POST requests. Chain the results using flatMap.',
    starterCode:'import org.springframework.web.reactive.function.client.WebClient;\nimport reactor.core.publisher.Mono;\nimport reactor.core.publisher.Flux;\n\npublic class WebClientDemo {\n    // TODO: Create WebClient instance\n    // TODO: GET request returning Mono<String>\n    // TODO: POST request with body returning Mono<String>\n}',
    requirements:{ patterns:[{ name:'WebClient', pattern:'WebClient', required:true },{ name:'.get()', pattern:'\\.get\\s*\\(', required:true },{ name:'.retrieve()', pattern:'\\.retrieve\\s*\\(', required:true }] },
    tests:[ { name:'WebClient with GET and retrieve', visible:true, validate:(c)=>({ passed:/WebClient/.test(c)&&/\.get\s*\(/.test(c)&&/\.retrieve\s*\(/.test(c), message:'Use WebClient.create().get().retrieve()' }) } ],
    hiddenTests:[], hints:['WebClient client = WebClient.create("http://api.example.com");','client.get().uri("/users").retrieve().bodyToMono(String.class)','For POST: client.post().uri("/users").bodyValue(user).retrieve()...'],
    solution:'import org.springframework.web.reactive.function.client.WebClient;\nimport reactor.core.publisher.Mono;\n\npublic class WebClientDemo {\n    private final WebClient client = WebClient.create("http://localhost:8080");\n\n    public Mono<String> getUser(int id) {\n        return client.get()\n            .uri("/users/{id}", id)\n            .retrieve()\n            .bodyToMono(String.class);\n    }\n\n    public Mono<String> createUser(String name) {\n        return client.post()\n            .uri("/users")\n            .bodyValue(new User(null, name, name + "@test.com"))\n            .retrieve()\n            .bodyToMono(String.class);\n    }\n\n    record User(Long id, String name, String email) {}\n}',
    explanation:'WebClient replaces RestTemplate in reactive apps. It is fully non-blocking and returns Mono/Flux. Use .get()/.post() to set method, .uri() for the URL, .bodyValue() for request body, .retrieve() to execute, and .bodyToMono()/.bodyToFlux() to parse response. Chain multiple WebClient calls with flatMap for orchestration.'
  },
  { id:'wf-005', worldId:'webflux-hq', order:5, title:'Reactive Service Layer', difficulty:'medium', xpReward:280, coinReward:60, estimatedTime:18,
    story:'A reactive controller without a reactive service is like a sports car with bicycle wheels. Build a proper @Service.',
    objective:'Create a @Service class with methods returning Mono and Flux. Inject it into a @RestController using constructor injection.',
    starterCode:'// TODO: Create ProductService with @Service\n// Methods: Mono<Product> findById(Long id), Flux<Product> findAll()\n\n// TODO: Create ProductController with @RestController\n// Inject ProductService via constructor\n// Map GET /products and GET /products/{id}',
    requirements:{ annotations:['Service','RestController','GetMapping'], patterns:[{ name:'constructor injection', pattern:'private\\s+final\\s+\\w+Service', required:true },{ name:'Mono return', pattern:'Mono<', required:true },{ name:'Flux return', pattern:'Flux<', required:true }] },
    tests:[ { name:'Service with reactive returns + constructor injection', visible:true, validate:(c)=>({ passed:/@Service/.test(c)&&/private\s+final/.test(c)&&/Mono</.test(c)&&/Flux</.test(c), message:'Create @Service with Mono/Flux methods and constructor injection' }) } ],
    hiddenTests:[], hints:['@Service class ProductService { Mono<Product> findById(Long id) {...} }','In controller: private final ProductService service; public ProductController(ProductService service) { this.service = service; }','Spring auto-injects via constructor — no @Autowired needed!'],
    solution:'import org.springframework.stereotype.Service;\nimport org.springframework.web.bind.annotation.*;\nimport reactor.core.publisher.Mono;\nimport reactor.core.publisher.Flux;\n\nrecord Product(Long id, String name, double price) {}\n\n@Service\nclass ProductService {\n    public Flux<Product> findAll() {\n        return Flux.just(new Product(1L,"Laptop",999.99), new Product(2L,"Phone",699.99));\n    }\n    public Mono<Product> findById(Long id) {\n        return Mono.just(new Product(id, "Product_" + id, 99.99));\n    }\n}\n\n@RestController\n@RequestMapping("/products")\nclass ProductController {\n    private final ProductService service;\n    public ProductController(ProductService service) { this.service = service; }\n\n    @GetMapping\n    public Flux<Product> getAll() { return service.findAll(); }\n\n    @GetMapping("/{id}")\n    public Mono<Product> getById(@PathVariable Long id) { return service.findById(id); }\n}',
    explanation:'Layered architecture applies in reactive too: Controller → Service → Repository. Constructor injection is preferred over @Autowired — it makes dependencies explicit and immutable (private final). Every method in the reactive service layer returns Mono or Flux — never block!'
  },
  { id:'wf-006', worldId:'webflux-hq', order:6, title:'R2DBC Reactive Repository', difficulty:'hard', xpReward:320, coinReward:70, estimatedTime:20,
    story:'JPA is blocking. For reactive database access, use R2DBC — Reactive Relational Database Connectivity.',
    objective:'Create a reactive repository interface extending ReactiveCrudRepository. Define custom query methods.',
    starterCode:'import org.springframework.data.repository.reactive.ReactiveCrudRepository;\nimport reactor.core.publisher.Flux;\nimport reactor.core.publisher.Mono;\n\n// TODO: Entity class with @Table annotation\n// TODO: Repository extending ReactiveCrudRepository\n// TODO: Custom query method: Flux<Product> findByPriceGreaterThan(double price)',
    requirements:{ patterns:[{ name:'ReactiveCrudRepository', pattern:'ReactiveCrudRepository', required:true },{ name:'@Table', pattern:'@Table', required:true },{ name:'Flux query method', pattern:'Flux<\\w+>\\s+findBy', required:true }] },
    tests:[ { name:'Reactive repository with custom query', visible:true, validate:(c)=>({ passed:/ReactiveCrudRepository/.test(c)&&/Flux<\w+>\s+findBy/.test(c)&&/@Table/.test(c), message:'Extend ReactiveCrudRepository with @Table entity' }) } ],
    hiddenTests:[], hints:['R2DBC uses @Table instead of @Entity, @Id for primary key.','public interface ProductRepo extends ReactiveCrudRepository<Product, Long>','findByPriceGreaterThan(double price) returns Flux<Product>'],
    solution:'import org.springframework.data.annotation.Id;\nimport org.springframework.data.relational.core.mapping.Table;\nimport org.springframework.data.repository.reactive.ReactiveCrudRepository;\nimport reactor.core.publisher.Flux;\n\n@Table("products")\nclass Product {\n    @Id\n    private Long id;\n    private String name;\n    private double price;\n    // constructors, getters, setters\n}\n\ninterface ProductRepository extends ReactiveCrudRepository<Product, Long> {\n    Flux<Product> findByPriceGreaterThan(double price);\n    Flux<Product> findByNameContaining(String keyword);\n}',
    explanation:'R2DBC is the reactive alternative to JDBC/JPA. ReactiveCrudRepository provides save(), findById(), findAll(), deleteById() — all returning Mono/Flux. Custom query methods follow Spring Data conventions (findByField). Use @Table (not @Entity) and spring-data-r2dbc dependency. The entire DB interaction is non-blocking.'
  },
  { id:'wf-007', worldId:'webflux-hq', order:7, title:'Reactive Error Handling', difficulty:'hard', xpReward:300, coinReward:65, estimatedTime:18,
    story:'Your reactive API needs proper error responses — 404 for not found, 400 for bad input, 500 for server errors.',
    objective:'Use @ExceptionHandler with reactive types. Create custom exceptions and return ResponseEntity<Mono<ErrorResponse>>.',
    starterCode:'import org.springframework.web.bind.annotation.*;\nimport org.springframework.http.*;\nimport reactor.core.publisher.Mono;\n\n// TODO: Custom exception classes\n// TODO: @RestControllerAdvice for global error handling\n// TODO: @ExceptionHandler methods returning Mono<ResponseEntity>',
    requirements:{ annotations:['RestControllerAdvice','ExceptionHandler'], patterns:[{ name:'ResponseEntity', pattern:'ResponseEntity', required:true },{ name:'HttpStatus', pattern:'HttpStatus', required:true }] },
    tests:[ { name:'Global error handler with ResponseEntity', visible:true, validate:(c)=>({ passed:/@RestControllerAdvice/.test(c)&&/@ExceptionHandler/.test(c)&&/ResponseEntity/.test(c), message:'Use @RestControllerAdvice with @ExceptionHandler' }) } ],
    hiddenTests:[], hints:['Create: class ResourceNotFoundException extends RuntimeException','@RestControllerAdvice class GlobalExceptionHandler','@ExceptionHandler(ResourceNotFoundException.class) returns Mono<ResponseEntity<ErrorResponse>>'],
    solution:'import org.springframework.web.bind.annotation.*;\nimport org.springframework.http.*;\nimport reactor.core.publisher.Mono;\n\nclass ResourceNotFoundException extends RuntimeException {\n    public ResourceNotFoundException(String msg) { super(msg); }\n}\n\nrecord ErrorResponse(int status, String message, long timestamp) {}\n\n@RestControllerAdvice\nclass GlobalExceptionHandler {\n    @ExceptionHandler(ResourceNotFoundException.class)\n    public Mono<ResponseEntity<ErrorResponse>> handleNotFound(ResourceNotFoundException ex) {\n        return Mono.just(ResponseEntity.status(HttpStatus.NOT_FOUND)\n            .body(new ErrorResponse(404, ex.getMessage(), System.currentTimeMillis())));\n    }\n\n    @ExceptionHandler(RuntimeException.class)\n    public Mono<ResponseEntity<ErrorResponse>> handleRuntime(RuntimeException ex) {\n        return Mono.just(ResponseEntity.status(HttpStatus.INTERNAL_SERVER_ERROR)\n            .body(new ErrorResponse(500, ex.getMessage(), System.currentTimeMillis())));\n    }\n}',
    explanation:'@RestControllerAdvice + @ExceptionHandler works identically in WebFlux as in MVC. Return Mono<ResponseEntity<T>> for reactive error responses. Create specific exception classes for different error scenarios. Always include timestamp, status code, and message in error responses — this is production standard.'
  },
  { id:'wf-008', worldId:'webflux-hq', order:8, title:'Reactive Validation', difficulty:'hard', xpReward:300, coinReward:65, estimatedTime:15,
    story:'Never trust user input. Validate request bodies reactively before processing.',
    objective:'Use @Valid with reactive @RequestBody. Create a custom validator and handle validation errors.',
    starterCode:'import org.springframework.web.bind.annotation.*;\nimport reactor.core.publisher.Mono;\nimport jakarta.validation.constraints.*;\nimport jakarta.validation.Valid;\n\n// TODO: DTO with validation annotations\n// TODO: Controller using @Valid @RequestBody Mono<CreateUserRequest>\n// TODO: Handle MethodArgumentNotValidException',
    requirements:{ patterns:[{ name:'@Valid', pattern:'@Valid', required:true },{ name:'@NotBlank/@NotNull', pattern:'@Not(Blank|Null|Empty)', required:true },{ name:'@RequestBody', pattern:'@RequestBody', required:true }] },
    tests:[ { name:'Validation annotations on DTO', visible:true, validate:(c)=>({ passed:/@Valid/.test(c)&&/@Not(Blank|Null|Empty)/.test(c)&&/@RequestBody/.test(c), message:'Use @Valid with validation constraints' }) } ],
    hiddenTests:[], hints:['@NotBlank(message="Name is required") on String fields','@Email for email validation, @Size(min=2,max=50) for length','Use @Valid before @RequestBody Mono<DTO> in controller method'],
    solution:'import org.springframework.web.bind.annotation.*;\nimport reactor.core.publisher.Mono;\nimport jakarta.validation.constraints.*;\nimport jakarta.validation.Valid;\n\nrecord CreateUserRequest(\n    @NotBlank(message = "Name is required")\n    @Size(min = 2, max = 50)\n    String name,\n    @NotBlank @Email(message = "Invalid email")\n    String email,\n    @Min(18) @Max(120)\n    int age\n) {}\n\n@RestController\n@RequestMapping("/users")\nclass UserController {\n    @PostMapping\n    public Mono<String> createUser(@Valid @RequestBody Mono<CreateUserRequest> request) {\n        return request.map(r -> "Created: " + r.name());\n    }\n}',
    explanation:'Bean validation (@NotBlank, @Email, @Size, @Min, @Max) works with reactive bodies when using @Valid. Spring validates the deserialized object and throws MethodArgumentNotValidException for invalid input. Combine with @RestControllerAdvice to return structured validation error responses.'
  },
  { id:'wf-009', worldId:'webflux-hq', order:9, title:'Reactive Testing with WebTestClient', difficulty:'hard', xpReward:320, coinReward:70, estimatedTime:18,
    story:'WebTestClient is the reactive replacement for MockMvc. Test your WebFlux endpoints without starting a real server.',
    objective:'Write tests using WebTestClient to test GET and POST endpoints. Assert status codes, response bodies, and headers.',
    starterCode:'import org.springframework.test.web.reactive.server.WebTestClient;\nimport org.springframework.boot.test.autoconfigure.web.reactive.WebFluxTest;\nimport org.junit.jupiter.api.Test;\n\n// TODO: @WebFluxTest(ProductController.class)\n// TODO: Inject WebTestClient\n// TODO: Test GET /products returns 200\n// TODO: Test POST /products with body returns 201\n// TODO: Test GET /products/999 returns 404',
    requirements:{ patterns:[{ name:'WebTestClient', pattern:'WebTestClient', required:true },{ name:'.expectStatus()', pattern:'\\.expectStatus\\s*\\(', required:true },{ name:'.expectBody()', pattern:'\\.expectBody', required:true }] },
    tests:[ { name:'WebTestClient with assertions', visible:true, validate:(c)=>({ passed:/WebTestClient/.test(c)&&/\.expectStatus/.test(c)&&/\.expectBody/.test(c), message:'Use WebTestClient.get().exchange().expectStatus().expectBody()' }) } ],
    hiddenTests:[], hints:['client.get().uri("/products").exchange().expectStatus().isOk()','client.post().uri("/products").bodyValue(dto).exchange().expectStatus().isCreated()','Use .expectBody(Product.class).value(p -> assert p.name().equals("test"))'],
    solution:'import org.springframework.test.web.reactive.server.WebTestClient;\nimport org.junit.jupiter.api.Test;\n\nclass ProductControllerTest {\n    private final WebTestClient client = WebTestClient.bindToController(new ProductController()).build();\n\n    @Test\n    void getProducts_returns200() {\n        client.get().uri("/products")\n            .exchange()\n            .expectStatus().isOk()\n            .expectBodyList(String.class).hasSize(2);\n    }\n\n    @Test\n    void createProduct_returns201() {\n        client.post().uri("/products")\n            .bodyValue(new Product(null, "Test", 9.99))\n            .exchange()\n            .expectStatus().isCreated()\n            .expectBody(Product.class)\n            .value(p -> { assert p.name().equals("Test"); });\n    }\n\n    @Test\n    void getProduct_notFound_returns404() {\n        client.get().uri("/products/999")\n            .exchange()\n            .expectStatus().isNotFound();\n    }\n}',
    explanation:'WebTestClient is the standard for testing WebFlux controllers. Use @WebFluxTest for slice testing. Chain: get/post → uri → exchange → expectStatus → expectBody. It supports both bound-to-controller (unit) and bound-to-server (integration) modes. Always test happy path AND error scenarios.'
  },
  { id:'wf-010', worldId:'webflux-hq', order:10, title:'🏆 Boss: Full Reactive CRUD API', difficulty:'boss', xpReward:550, coinReward:140, estimatedTime:30,
    story:'⚔️ FINAL BOSS! Build a complete reactive CRUD API with Controller → Service → Repository layers, validation, error handling, and proper HTTP status codes.',
    objective:'Build a full reactive API for "Employee" with: POST (201), GET all (200), GET by id (200/404), PUT (200/404), DELETE (204/404). Include @Valid, @ExceptionHandler, and proper ResponseEntity usage.',
    starterCode:'// Build complete Employee CRUD API\n// Layers: Controller → Service → Repository\n// Entity: Employee(Long id, String name, String dept, double salary)\n// Endpoints:\n//   POST   /api/employees       → 201 Created\n//   GET    /api/employees       → 200 OK (Flux)\n//   GET    /api/employees/{id}  → 200 OK / 404\n//   PUT    /api/employees/{id}  → 200 OK / 404\n//   DELETE /api/employees/{id}  → 204 No Content / 404\n// Include: @Valid, @RestControllerAdvice, ResponseEntity',
    requirements:{ annotations:['RestController','Service','GetMapping','PostMapping','PutMapping','DeleteMapping','Valid','RestControllerAdvice','ExceptionHandler','RequestBody','PathVariable'], patterns:[{ name:'Mono<ResponseEntity>', pattern:'Mono<ResponseEntity', required:true },{ name:'Flux<Employee>', pattern:'Flux<\\w*Employee', required:true }] },
    tests:[ { name:'All 5 CRUD endpoints', visible:true, validate:(c)=>{ const ops=['@GetMapping','@PostMapping','@PutMapping','@DeleteMapping']; const f=ops.filter(o=>c.includes(o)); return { passed:f.length===4, message:`Found ${f.length}/4 HTTP methods` }; } },
      { name:'Full layered architecture', visible:true, validate:(c)=>({ passed:/@Service/.test(c)&&/@RestController/.test(c)&&/@RestControllerAdvice/.test(c), message:'Need @Service + @RestController + @RestControllerAdvice' }) }
    ], hiddenTests:[], hints:['Use Mono<ResponseEntity<Employee>> for proper status codes.','switchIfEmpty(Mono.error(new NotFoundException())) for 404 handling.','@DeleteMapping returns Mono<ResponseEntity<Void>> with .noContent().build()'],
    solution:'// Full solution in explanation — this is a comprehensive multi-class implementation',
    explanation:'A production reactive CRUD API has: (1) Entity/DTO with validation, (2) Repository extending ReactiveCrudRepository, (3) Service with business logic returning Mono/Flux, (4) Controller mapping HTTP to service calls with proper ResponseEntity, (5) Global exception handler. Key patterns: switchIfEmpty for 404, ResponseEntity.created(URI) for 201, ResponseEntity.noContent() for 204. This is the exact architecture used in production Spring WebFlux microservices.'
  }
];
