export const ENTERPRISE_TOWER_MISSIONS = [
  { id:'et-001', worldId:'enterprise-tower', order:1, title:'Spring Security Basics', difficulty:'medium', xpReward:280, coinReward:60, estimatedTime:15,
    story:'Enterprise apps need security. Configure Spring Security with SecurityFilterChain.',
    objective:'Create a @Configuration class with @EnableWebSecurity. Define a SecurityFilterChain @Bean that permits /public/**, requires auth for everything else, and enables httpBasic.',
    starterCode:'import org.springframework.context.annotation.Bean;\nimport org.springframework.context.annotation.Configuration;\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\nimport org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;\nimport org.springframework.security.web.SecurityFilterChain;\n\n// TODO: @Configuration @EnableWebSecurity\npublic class SecurityConfig {\n    // TODO: @Bean SecurityFilterChain\n}',
    requirements:{ annotations:['Configuration','EnableWebSecurity','Bean'], patterns:[{ name:'SecurityFilterChain', pattern:'SecurityFilterChain', required:true },{ name:'authorizeHttpRequests', pattern:'authorizeHttpRequests|authorizeRequests', required:true }] },
    tests:[ { name:'Security config with filter chain', visible:true, validate:(c)=>({ passed:/@Configuration/.test(c)&&/SecurityFilterChain/.test(c)&&/@Bean/.test(c), message:'Need @Configuration + @Bean SecurityFilterChain' }) } ],
    hiddenTests:[], hints:['@Bean public SecurityFilterChain filterChain(HttpSecurity http)','http.authorizeHttpRequests(auth -> auth.requestMatchers("/public/**").permitAll().anyRequest().authenticated())','End with: .httpBasic(Customizer.withDefaults()).build()'],
    solution:'@Configuration\n@EnableWebSecurity\npublic class SecurityConfig {\n    @Bean\n    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n        return http\n            .authorizeHttpRequests(auth -> auth\n                .requestMatchers("/public/**").permitAll()\n                .anyRequest().authenticated())\n            .httpBasic(Customizer.withDefaults())\n            .build();\n    }\n}',
    explanation:'Spring Security uses a filter chain to intercept requests. @EnableWebSecurity activates it. SecurityFilterChain @Bean defines rules: permitAll() for public endpoints, authenticated() for protected ones. httpBasic enables Basic Auth. In production, replace with JWT.'
  },
  { id:'et-002', worldId:'enterprise-tower', order:2, title:'JWT Authentication', difficulty:'hard', xpReward:350, coinReward:80, estimatedTime:20,
    story:'Basic auth is not enough. Implement JWT token generation and validation for stateless authentication.',
    objective:'Create a JwtUtil class with generateToken(username) and validateToken(token) methods. Use io.jsonwebtoken (JJWT) library patterns.',
    starterCode:'import java.util.Date;\n// Assume io.jsonwebtoken.* is available\n\npublic class JwtUtil {\n    private final String SECRET_KEY = "mySecretKey12345678901234567890123";\n    private final long EXPIRATION = 3600000; // 1 hour\n\n    // TODO: generateToken(String username) -> String\n    // TODO: extractUsername(String token) -> String\n    // TODO: validateToken(String token, String username) -> boolean\n}',
    requirements:{ patterns:[{ name:'generateToken', pattern:'generateToken', required:true },{ name:'validateToken', pattern:'validateToken', required:true },{ name:'Jwts.builder', pattern:'Jwts\\.builder|jwt|token', required:true },{ name:'SECRET_KEY', pattern:'SECRET|secret|key', required:true }] },
    tests:[ { name:'JWT generation and validation', visible:true, validate:(c)=>({ passed:/generateToken/.test(c)&&/validateToken/.test(c)&&/Date|expir/i.test(c), message:'Implement token generation with expiration and validation' }) } ],
    hiddenTests:[], hints:['Jwts.builder().setSubject(username).setIssuedAt(new Date()).setExpiration(...)','Use .signWith(SignatureAlgorithm.HS256, SECRET_KEY)','validateToken checks: username matches AND token not expired'],
    solution:'public class JwtUtil {\n    private final String SECRET_KEY = "mySecretKey12345678901234567890123";\n    private final long EXPIRATION = 3600000;\n\n    public String generateToken(String username) {\n        return Jwts.builder()\n            .setSubject(username)\n            .setIssuedAt(new Date())\n            .setExpiration(new Date(System.currentTimeMillis() + EXPIRATION))\n            .signWith(SignatureAlgorithm.HS256, SECRET_KEY)\n            .compact();\n    }\n\n    public String extractUsername(String token) {\n        return Jwts.parser().setSigningKey(SECRET_KEY).parseClaimsJws(token).getBody().getSubject();\n    }\n\n    public boolean validateToken(String token, String username) {\n        String extracted = extractUsername(token);\n        return extracted.equals(username) && !isTokenExpired(token);\n    }\n\n    private boolean isTokenExpired(String token) {\n        Date exp = Jwts.parser().setSigningKey(SECRET_KEY).parseClaimsJws(token).getBody().getExpiration();\n        return exp.before(new Date());\n    }\n}',
    explanation:'JWT (JSON Web Token) enables stateless auth. The server generates a signed token with claims (subject, expiration). Clients send it in Authorization: Bearer <token>. The server validates the signature and expiration — no session storage needed. This is the industry standard for REST API authentication.'
  },
  { id:'et-003', worldId:'enterprise-tower', order:3, title:'@Cacheable & Caching', difficulty:'medium', xpReward:270, coinReward:55, estimatedTime:15,
    story:'Database calls are expensive. Cache frequently accessed data using Spring\'s @Cacheable.',
    objective:'Add @EnableCaching to config. Use @Cacheable, @CachePut, and @CacheEvict on service methods.',
    starterCode:'import org.springframework.cache.annotation.*;\nimport org.springframework.stereotype.Service;\n\n// TODO: @EnableCaching on config class\n// TODO: @Service with @Cacheable, @CachePut, @CacheEvict methods',
    requirements:{ annotations:['EnableCaching','Cacheable'], patterns:[{ name:'@CachePut or @CacheEvict', pattern:'@Cache(Put|Evict)', required:true }] },
    tests:[ { name:'All 3 cache annotations', visible:true, validate:(c)=>({ passed:/@Cacheable/.test(c)&&/@CachePut|@CacheEvict/.test(c)&&/@EnableCaching/.test(c), message:'Use @EnableCaching + @Cacheable + @CachePut/@CacheEvict' }) } ],
    hiddenTests:[], hints:['@Cacheable("products") caches the return value by method args.','@CachePut("products") updates the cache entry.','@CacheEvict("products") removes from cache.'],
    solution:'@Configuration\n@EnableCaching\npublic class CacheConfig {}\n\n@Service\npublic class ProductService {\n    @Cacheable(value = "products", key = "#id")\n    public Product findById(Long id) {\n        return repository.findById(id).orElseThrow();\n    }\n\n    @CachePut(value = "products", key = "#product.id")\n    public Product update(Product product) {\n        return repository.save(product);\n    }\n\n    @CacheEvict(value = "products", key = "#id")\n    public void delete(Long id) {\n        repository.deleteById(id);\n    }\n}',
    explanation:'@Cacheable stores method results — subsequent calls with same args return cached data. @CachePut always executes and updates cache. @CacheEvict removes entries. Use cache for read-heavy data. In production, use Redis or Caffeine as cache provider instead of the default ConcurrentHashMap.'
  },
  { id:'et-004', worldId:'enterprise-tower', order:4, title:'@Async & @Scheduled', difficulty:'medium', xpReward:270, coinReward:55, estimatedTime:15,
    story:'Some tasks should run in background threads or on a schedule — email sending, report generation, cleanup jobs.',
    objective:'Use @EnableAsync + @Async for background execution. Use @EnableScheduling + @Scheduled for periodic tasks.',
    starterCode:'// TODO: Config with @EnableAsync @EnableScheduling\n// TODO: Service with @Async method returning CompletableFuture\n// TODO: Scheduled task with @Scheduled(fixedRate = 5000)',
    requirements:{ annotations:['EnableAsync','Async','EnableScheduling','Scheduled'], patterns:[{ name:'CompletableFuture', pattern:'CompletableFuture', required:true }] },
    tests:[ { name:'Async and Scheduled annotations', visible:true, validate:(c)=>({ passed:/@Async/.test(c)&&/@Scheduled/.test(c)&&/CompletableFuture/.test(c), message:'Use @Async with CompletableFuture + @Scheduled' }) } ],
    hiddenTests:[], hints:['@Async public CompletableFuture<String> sendEmail() { ... }','@Scheduled(fixedRate = 5000) runs every 5 seconds.','@Scheduled(cron = "0 0 2 * * ?") runs at 2 AM daily.'],
    solution:'@Configuration\n@EnableAsync\n@EnableScheduling\npublic class AsyncConfig {}\n\n@Service\npublic class NotificationService {\n    @Async\n    public CompletableFuture<String> sendEmail(String to) {\n        // simulate slow operation\n        try { Thread.sleep(2000); } catch (Exception e) {}\n        return CompletableFuture.completedFuture("Sent to " + to);\n    }\n\n    @Scheduled(fixedRate = 60000)\n    public void cleanupExpiredTokens() {\n        System.out.println("Cleaning expired tokens at " + java.time.LocalDateTime.now());\n    }\n\n    @Scheduled(cron = "0 0 2 * * ?")\n    public void generateDailyReport() {\n        System.out.println("Generating daily report...");\n    }\n}',
    explanation:'@Async executes methods in a separate thread pool — caller does not block. Return CompletableFuture for async results. @Scheduled runs methods on a timer: fixedRate (ms between starts), fixedDelay (ms after completion), cron (Unix-style schedule). Essential for background processing in production.'
  },
  { id:'et-005', worldId:'enterprise-tower', order:5, title:'Event-Driven with ApplicationEvent', difficulty:'medium', xpReward:280, coinReward:60, estimatedTime:15,
    story:'Tightly coupled services are fragile. Use Spring Events to decouple components — when an order is placed, multiple listeners react independently.',
    objective:'Create a custom ApplicationEvent, publish it with ApplicationEventPublisher, and handle it with @EventListener.',
    starterCode:'import org.springframework.context.ApplicationEvent;\nimport org.springframework.context.ApplicationEventPublisher;\nimport org.springframework.context.event.EventListener;\n\n// TODO: Create OrderPlacedEvent extending ApplicationEvent\n// TODO: Publish event in OrderService\n// TODO: Listen with @EventListener in NotificationService and InventoryService',
    requirements:{ patterns:[{ name:'ApplicationEvent', pattern:'ApplicationEvent|extends.*Event', required:true },{ name:'ApplicationEventPublisher', pattern:'ApplicationEventPublisher|publisher\\.publish', required:true },{ name:'@EventListener', pattern:'@EventListener', required:true }] },
    tests:[ { name:'Event publishing and listening', visible:true, validate:(c)=>({ passed:/ApplicationEvent|extends.*Event/.test(c)&&/@EventListener/.test(c)&&/publish/.test(c), message:'Create event class + publisher + @EventListener' }) } ],
    hiddenTests:[], hints:['class OrderPlacedEvent extends ApplicationEvent { private final Order order; ... }','publisher.publishEvent(new OrderPlacedEvent(this, order));','@EventListener public void onOrderPlaced(OrderPlacedEvent event) { ... }'],
    solution:'class OrderPlacedEvent extends ApplicationEvent {\n    private final String orderId;\n    public OrderPlacedEvent(Object source, String orderId) {\n        super(source);\n        this.orderId = orderId;\n    }\n    public String getOrderId() { return orderId; }\n}\n\n@Service\nclass OrderService {\n    private final ApplicationEventPublisher publisher;\n    public OrderService(ApplicationEventPublisher publisher) { this.publisher = publisher; }\n\n    public void placeOrder(String orderId) {\n        // save order...\n        publisher.publishEvent(new OrderPlacedEvent(this, orderId));\n    }\n}\n\n@Component\nclass NotificationListener {\n    @EventListener\n    public void onOrderPlaced(OrderPlacedEvent event) {\n        System.out.println("Sending notification for order: " + event.getOrderId());\n    }\n}\n\n@Component\nclass InventoryListener {\n    @EventListener\n    public void onOrderPlaced(OrderPlacedEvent event) {\n        System.out.println("Updating inventory for order: " + event.getOrderId());\n    }\n}',
    explanation:'Spring Events implement the Observer pattern. The publisher fires an event, and any number of listeners react — completely decoupled. Add @Async to @EventListener for non-blocking listeners. This is the foundation of event-driven architecture and eventually leads to message brokers like Kafka/RabbitMQ in microservices.'
  },
  { id:'et-006', worldId:'enterprise-tower', order:6, title:'Microservices Communication', difficulty:'hard', xpReward:350, coinReward:80, estimatedTime:20,
    story:'In a microservices architecture, services communicate via REST (synchronous) or messaging (asynchronous). Build inter-service communication.',
    objective:'Use WebClient for sync service-to-service calls. Implement service discovery patterns with @FeignClient or manual WebClient configuration.',
    starterCode:'// Order Service calls Product Service and User Service\n// TODO: WebClient-based service client\n// TODO: Combine results from multiple services using Mono.zip\n// TODO: Handle service unavailability with fallbacks',
    requirements:{ patterns:[{ name:'WebClient', pattern:'WebClient', required:true },{ name:'Mono.zip or flatMap', pattern:'Mono\\.zip|\\.flatMap', required:true },{ name:'onErrorResume/fallback', pattern:'onErrorResume|onErrorReturn|fallback', required:true }] },
    tests:[ { name:'Inter-service communication with fallback', visible:true, validate:(c)=>({ passed:/WebClient/.test(c)&&/(Mono\.zip|\.flatMap)/.test(c)&&/(onErrorResume|onErrorReturn|fallback)/.test(c), message:'WebClient + Mono.zip/flatMap + error fallback' }) } ],
    hiddenTests:[], hints:['Create a dedicated client class per service: ProductServiceClient','Use Mono.zip(getProduct(id), getUser(userId)) to combine parallel calls.','Add .onErrorResume(e -> Mono.just(fallbackProduct)) for resilience.'],
    solution:'@Component\nclass ProductServiceClient {\n    private final WebClient client;\n    public ProductServiceClient(WebClient.Builder builder) {\n        this.client = builder.baseUrl("http://product-service:8081").build();\n    }\n    public Mono<Product> getProduct(Long id) {\n        return client.get().uri("/api/products/{id}", id)\n            .retrieve().bodyToMono(Product.class)\n            .onErrorResume(e -> Mono.just(new Product(id, "Unavailable", 0)));\n    }\n}\n\n@Service\nclass OrderService {\n    private final ProductServiceClient productClient;\n    private final UserServiceClient userClient;\n\n    public Mono<OrderDetails> getOrderDetails(Long orderId) {\n        return Mono.zip(\n            productClient.getProduct(orderId),\n            userClient.getUser(orderId),\n            (product, user) -> new OrderDetails(orderId, product, user)\n        );\n    }\n}',
    explanation:'Microservices communicate via HTTP (WebClient) or messaging (Kafka/RabbitMQ). WebClient is non-blocking and perfect for service-to-service calls. Mono.zip enables parallel calls to multiple services. Always add fallbacks — in microservices, any service can be down. This pattern is used at Netflix, Uber, and every major tech company.'
  },
  { id:'et-007', worldId:'enterprise-tower', order:7, title:'Circuit Breaker Pattern', difficulty:'hard', xpReward:350, coinReward:80, estimatedTime:18,
    story:'When a downstream service is failing, you don\'t want to keep hammering it. Circuit breaker stops cascading failures.',
    objective:'Implement the circuit breaker pattern using Resilience4j annotations: @CircuitBreaker with fallback method, @Retry, @RateLimiter.',
    starterCode:'import io.github.resilience4j.circuitbreaker.annotation.CircuitBreaker;\nimport io.github.resilience4j.retry.annotation.Retry;\n\n// TODO: Service with @CircuitBreaker(name="productService", fallbackMethod="fallback")\n// TODO: Fallback method with same signature + Exception parameter\n// TODO: @Retry for automatic retries',
    requirements:{ patterns:[{ name:'@CircuitBreaker', pattern:'@CircuitBreaker', required:true },{ name:'fallbackMethod', pattern:'fallbackMethod', required:true },{ name:'@Retry', pattern:'@Retry', required:true }] },
    tests:[ { name:'Circuit breaker with fallback and retry', visible:true, validate:(c)=>({ passed:/@CircuitBreaker/.test(c)&&/fallbackMethod/.test(c)&&/@Retry/.test(c), message:'Use @CircuitBreaker with fallbackMethod + @Retry' }) } ],
    hiddenTests:[], hints:['@CircuitBreaker(name="myService", fallbackMethod="myFallback")','Fallback method: same return type, same params + Exception','@Retry(name="myService", maxAttempts=3) retries on failure'],
    solution:'@Service\npublic class ProductServiceClient {\n    private final WebClient webClient;\n\n    @CircuitBreaker(name = "productService", fallbackMethod = "getProductFallback")\n    @Retry(name = "productService")\n    public Mono<Product> getProduct(Long id) {\n        return webClient.get().uri("/products/{id}", id)\n            .retrieve().bodyToMono(Product.class);\n    }\n\n    public Mono<Product> getProductFallback(Long id, Exception e) {\n        return Mono.just(new Product(id, "Cached/Default Product", 0.0));\n    }\n}',
    explanation:'Circuit Breaker has 3 states: CLOSED (normal), OPEN (failing — returns fallback immediately), HALF_OPEN (testing recovery). Resilience4j is the standard library. @Retry auto-retries failed calls. @RateLimiter prevents overwhelming services. This prevents cascade failures where one failing service brings down the entire system.'
  },
  { id:'et-008', worldId:'enterprise-tower', order:8, title:'API Gateway & Config Patterns', difficulty:'hard', xpReward:320, coinReward:70, estimatedTime:18,
    story:'A microservices system needs a single entry point (API Gateway) and externalized configuration.',
    objective:'Configure Spring Cloud Gateway routes. Use @ConfigurationProperties for type-safe external config. Use @Value for simple properties.',
    starterCode:'import org.springframework.boot.context.properties.ConfigurationProperties;\nimport org.springframework.beans.factory.annotation.Value;\n\n// TODO: @ConfigurationProperties(prefix="app") class\n// TODO: Use @Value for simple injection\n// TODO: Gateway route configuration bean',
    requirements:{ annotations:['ConfigurationProperties','Value'], patterns:[{ name:'prefix binding', pattern:'prefix|ConfigurationProperties', required:true },{ name:'Gateway route', pattern:'RouteLocator|route|gateway', required:true }] },
    tests:[ { name:'ConfigurationProperties and Gateway routes', visible:true, validate:(c)=>({ passed:/@ConfigurationProperties/.test(c)&&/(RouteLocator|route|gateway)/i.test(c), message:'Use @ConfigurationProperties + Gateway routes' }) } ],
    hiddenTests:[], hints:['@ConfigurationProperties(prefix="app") with fields matching YAML.','@Bean RouteLocator routes(RouteLocatorBuilder builder)','builder.routes().route("product-service", r -> r.path("/api/products/**").uri("lb://product-service"))'],
    solution:'@ConfigurationProperties(prefix = "app")\npublic class AppProperties {\n    private String name;\n    private String version;\n    private int maxRetries;\n    // getters, setters\n}\n\n@Configuration\npublic class GatewayConfig {\n    @Value("${app.name}")\n    private String appName;\n\n    @Bean\n    public RouteLocator routes(RouteLocatorBuilder builder) {\n        return builder.routes()\n            .route("product-service", r -> r.path("/api/products/**")\n                .filters(f -> f.stripPrefix(1).retry(3))\n                .uri("lb://product-service"))\n            .route("order-service", r -> r.path("/api/orders/**")\n                .uri("lb://order-service"))\n            .build();\n    }\n}',
    explanation:'API Gateway is the single entry point for all clients. It handles routing, auth, rate limiting. Spring Cloud Gateway uses reactive Netty. @ConfigurationProperties binds YAML/properties to POJOs — type-safe and validated. @Value injects single properties. Externalized config is critical for 12-factor apps and different environments (dev/staging/prod).'
  },
  { id:'et-009', worldId:'enterprise-tower', order:9, title:'Observability: Actuator & Metrics', difficulty:'medium', xpReward:280, coinReward:60, estimatedTime:15,
    story:'You can\'t fix what you can\'t see. Add health checks, metrics, and observability to your Spring Boot app.',
    objective:'Configure Spring Boot Actuator endpoints. Create custom health indicators and metrics with Micrometer.',
    starterCode:'import org.springframework.boot.actuate.health.*;\nimport io.micrometer.core.instrument.MeterRegistry;\n\n// TODO: Custom HealthIndicator\n// TODO: Custom metrics with MeterRegistry\n// TODO: application.properties for actuator exposure',
    requirements:{ patterns:[{ name:'HealthIndicator', pattern:'HealthIndicator|AbstractHealthIndicator', required:true },{ name:'Health.up/down', pattern:'Health\\.(up|down|status)', required:true },{ name:'MeterRegistry or Counter', pattern:'MeterRegistry|Counter|Timer|Gauge', required:true }] },
    tests:[ { name:'Custom health and metrics', visible:true, validate:(c)=>({ passed:/HealthIndicator|AbstractHealthIndicator/.test(c)&&/MeterRegistry|Counter|Timer/.test(c), message:'Implement HealthIndicator + custom metrics' }) } ],
    hiddenTests:[], hints:['Implement HealthIndicator interface with health() method.','Return Health.up().withDetail("db","connected").build()','MeterRegistry counter: registry.counter("api.calls", "endpoint", "/products").increment()'],
    solution:'@Component\npublic class DatabaseHealthIndicator implements HealthIndicator {\n    @Override\n    public Health health() {\n        boolean dbUp = checkDatabaseConnection();\n        return dbUp ? Health.up().withDetail("db","connected").build()\n                    : Health.down().withDetail("db","disconnected").build();\n    }\n    private boolean checkDatabaseConnection() { return true; }\n}\n\n@Service\npublic class ProductService {\n    private final Counter productViewCounter;\n    private final Timer responseTimer;\n\n    public ProductService(MeterRegistry registry) {\n        this.productViewCounter = registry.counter("products.views");\n        this.responseTimer = registry.timer("products.response.time");\n    }\n\n    public Product getProduct(Long id) {\n        return responseTimer.record(() -> {\n            productViewCounter.increment();\n            return findById(id);\n        });\n    }\n}',
    explanation:'Actuator exposes /health, /metrics, /info endpoints. Custom HealthIndicators check dependencies (DB, cache, external APIs). Micrometer provides Counters (how many), Timers (how long), Gauges (current value). Connect to Prometheus + Grafana for production monitoring. This is mandatory for production microservices.'
  },
  { id:'et-010', worldId:'enterprise-tower', order:10, title:'🏆 Boss: Production Microservice', difficulty:'boss', xpReward:600, coinReward:150, estimatedTime:30,
    story:'⚔️ FINAL BOSS OF SPRINGQUEST! Build a production-ready microservice with EVERY enterprise pattern you\'ve learned.',
    objective:'Build an Order Service with: @RestController, @Service, reactive repository, JWT security, @Cacheable, @Async email notification, @CircuitBreaker for external calls, custom health check, proper error handling, and @ConfigurationProperties.',
    starterCode:'// FINAL BOSS: Production-Ready Order Service\n// Include ALL of these:\n// 1. @RestController with CRUD endpoints\n// 2. @Service with business logic\n// 3. Repository layer (reactive)\n// 4. JWT authentication\n// 5. @Cacheable for read operations\n// 6. @Async for email notifications\n// 7. @CircuitBreaker for payment service call\n// 8. Custom HealthIndicator\n// 9. @RestControllerAdvice error handling\n// 10. @ConfigurationProperties',
    requirements:{ annotations:['RestController','Service','Cacheable','Async','CircuitBreaker','RestControllerAdvice','ExceptionHandler','ConfigurationProperties'], patterns:[{ name:'JWT/Security', pattern:'jwt|JWT|token|Token|Security', required:true },{ name:'HealthIndicator', pattern:'HealthIndicator|Health', required:true },{ name:'Reactive types', pattern:'Mono<|Flux<', required:true }] },
    tests:[ { name:'All enterprise patterns present', visible:true, validate:(c)=>{ const p=['@RestController','@Service','@Cacheable','@Async','@CircuitBreaker','@RestControllerAdvice','HealthIndicator']; const f=p.filter(x=>c.includes(x)); return { passed:f.length>=6, message:`Found ${f.length}/7 patterns: ${f.join(', ')}` }; } } ],
    hiddenTests:[], hints:['Start with the controller and service layer.','Add @Cacheable to findById, @Async to sendNotification.','Wrap payment call with @CircuitBreaker and fallbackMethod.'],
    solution:'// This is a comprehensive multi-class production microservice.\n// See explanation for the full architecture.',
    explanation:'Congratulations! A production microservice combines ALL these patterns: Layered architecture (Controller→Service→Repository), Security (JWT + SecurityFilterChain), Performance (@Cacheable, @Async), Resilience (@CircuitBreaker, @Retry), Observability (HealthIndicator, Micrometer), Configuration (@ConfigurationProperties), Error Handling (@RestControllerAdvice). You now have the skills to build, deploy, and maintain production Spring Boot microservices. You think like an engineer, not a tutorial watcher. 🏆'
  }
];
