export const REST_API_MISSIONS = [
  { id:'ra-001', worldId:'rest-api-dept', order:1, title:'@RestController Basics', difficulty:'easy', xpReward:200, coinReward:40, estimatedTime:10,
    story:'Time to build REST APIs! @RestController = @Controller + @ResponseBody — it returns data, not views.',
    objective:'Create a @RestController with @RequestMapping("/api"). Add a @GetMapping that returns a simple message.',
    starterCode:'// TODO: @RestController\n// TODO: @RequestMapping("/api")\npublic class ApiController {\n    // TODO: @GetMapping("/status")\n    // Return a status message\n}',
    requirements:{ annotations:['RestController','RequestMapping','GetMapping'] },
    tests:[{ name:'REST controller structure', visible:true, validate:(c)=>({ passed:/@RestController/.test(c)&&/@RequestMapping/.test(c)&&/@GetMapping/.test(c), message:'Add all 3 annotations' }) }],
    hiddenTests:[], hints:['@RestController tells Spring this class handles HTTP and returns JSON.','@RequestMapping("/api") sets the base path for all endpoints.','@GetMapping("/status") maps GET /api/status.'],
    solution:'@RestController\n@RequestMapping("/api")\npublic class ApiController {\n    @GetMapping("/status")\n    public String getStatus() {\n        return "API is running!";\n    }\n}',
    explanation:'@RestController = @Controller + @ResponseBody. Every method return value is serialized to JSON (via Jackson). @RequestMapping sets a base path. @GetMapping is shorthand for @RequestMapping(method=GET).'
  },
  { id:'ra-002', worldId:'rest-api-dept', order:2, title:'@PostMapping & @RequestBody', difficulty:'easy', xpReward:220, coinReward:45, estimatedTime:12,
    story:'GET reads data, POST creates data. Accept JSON request bodies with @RequestBody.',
    objective:'Create a @PostMapping that accepts a User object via @RequestBody and returns it with a generated ID.',
    starterCode:'import org.springframework.web.bind.annotation.*;\n\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    // TODO: @PostMapping accepting @RequestBody User\n    // Return the user with an assigned ID\n}',
    requirements:{ annotations:['PostMapping','RequestBody'], patterns:[{ name:'User class/record', pattern:'class User|record User', required:true }] },
    tests:[{ name:'POST with RequestBody', visible:true, validate:(c)=>({ passed:/@PostMapping/.test(c)&&/@RequestBody/.test(c), message:'Use @PostMapping with @RequestBody' }) }],
    hiddenTests:[], hints:['@PostMapping public User createUser(@RequestBody User user)','Spring deserializes JSON body into User object automatically.','Return the user object — Spring serializes it back to JSON.'],
    solution:'record User(Long id, String name, String email) {}\n\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    private long nextId = 1;\n\n    @PostMapping\n    public User createUser(@RequestBody User user) {\n        return new User(nextId++, user.name(), user.email());\n    }\n}',
    explanation:'@RequestBody tells Spring to deserialize the HTTP body (JSON) into a Java object using Jackson. Spring auto-serializes the return value back to JSON. This is the foundation of every REST API.'
  },
  { id:'ra-003', worldId:'rest-api-dept', order:3, title:'@PathVariable & @RequestParam', difficulty:'easy', xpReward:220, coinReward:45, estimatedTime:12,
    story:'APIs need dynamic URLs (/users/123) and query params (/users?role=admin).',
    objective:'Create endpoints using @PathVariable for /users/{id} and @RequestParam for /users?role=admin&active=true.',
    starterCode:'@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    // TODO: GET /api/users/{id} using @PathVariable\n    // TODO: GET /api/users?role=X&active=Y using @RequestParam\n}',
    requirements:{ annotations:['PathVariable','RequestParam','GetMapping'] },
    tests:[{ name:'PathVariable and RequestParam', visible:true, validate:(c)=>({ passed:/@PathVariable/.test(c)&&/@RequestParam/.test(c), message:'Use both @PathVariable and @RequestParam' }) }],
    hiddenTests:[], hints:['@GetMapping("/{id}") public User getById(@PathVariable Long id)','@GetMapping public List<User> search(@RequestParam String role)','@RequestParam(required=false, defaultValue="true") boolean active'],
    solution:'@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    @GetMapping("/{id}")\n    public String getById(@PathVariable Long id) {\n        return "User #" + id;\n    }\n\n    @GetMapping\n    public String search(\n        @RequestParam String role,\n        @RequestParam(required = false, defaultValue = "true") boolean active\n    ) {\n        return "Searching role=" + role + " active=" + active;\n    }\n}',
    explanation:'@PathVariable extracts from URL path: /users/{id}. @RequestParam extracts from query string: ?role=admin. Use required=false and defaultValue for optional params. These are the two primary ways to pass data in GET requests.'
  },
  { id:'ra-004', worldId:'rest-api-dept', order:4, title:'@PutMapping & @DeleteMapping', difficulty:'medium', xpReward:240, coinReward:50, estimatedTime:12,
    story:'CRUD needs Update and Delete. PUT replaces a resource, DELETE removes it.',
    objective:'Add @PutMapping for updating and @DeleteMapping for removing. Return proper ResponseEntity with status codes.',
    starterCode:'@RestController\n@RequestMapping("/api/products")\npublic class ProductController {\n    // TODO: @PutMapping("/{id}") with @RequestBody and @PathVariable\n    // TODO: @DeleteMapping("/{id}") with @PathVariable\n    // Use ResponseEntity for proper status codes\n}',
    requirements:{ annotations:['PutMapping','DeleteMapping','PathVariable'], patterns:[{ name:'ResponseEntity', pattern:'ResponseEntity', required:true }] },
    tests:[{ name:'PUT and DELETE with ResponseEntity', visible:true, validate:(c)=>({ passed:/@PutMapping/.test(c)&&/@DeleteMapping/.test(c)&&/ResponseEntity/.test(c), message:'Use @PutMapping + @DeleteMapping + ResponseEntity' }) }],
    hiddenTests:[], hints:['@PutMapping("/{id}") public ResponseEntity<Product> update(@PathVariable Long id, @RequestBody Product p)','@DeleteMapping("/{id}") returns ResponseEntity.noContent().build()','ResponseEntity.ok(updatedProduct) for 200, .notFound().build() for 404.'],
    solution:'@RestController\n@RequestMapping("/api/products")\npublic class ProductController {\n    @PutMapping("/{id}")\n    public ResponseEntity<Product> update(@PathVariable Long id, @RequestBody Product product) {\n        Product updated = new Product(id, product.name(), product.price());\n        return ResponseEntity.ok(updated);\n    }\n\n    @DeleteMapping("/{id}")\n    public ResponseEntity<Void> delete(@PathVariable Long id) {\n        return ResponseEntity.noContent().build();\n    }\n}',
    explanation:'PUT replaces an entire resource (use PATCH for partial updates). DELETE removes a resource. ResponseEntity gives you control over HTTP status: 200 OK, 201 Created, 204 No Content, 404 Not Found. Always use proper HTTP status codes — it\'s REST best practice.'
  },
  { id:'ra-005', worldId:'rest-api-dept', order:5, title:'@Valid & Bean Validation', difficulty:'medium', xpReward:250, coinReward:50, estimatedTime:15,
    story:'Never trust client input! Validate request data using Jakarta Bean Validation annotations.',
    objective:'Add @NotBlank, @Email, @Min, @Max, @Size to a DTO. Use @Valid in the controller to trigger validation.',
    starterCode:'import jakarta.validation.constraints.*;\nimport jakarta.validation.Valid;\n\n// TODO: CreateUserRequest with validation annotations\n// TODO: Controller using @Valid @RequestBody',
    requirements:{ patterns:[{ name:'@Valid', pattern:'@Valid', required:true },{ name:'@NotBlank', pattern:'@NotBlank|@NotNull|@NotEmpty', required:true },{ name:'@Email or @Size', pattern:'@Email|@Size|@Min|@Max', required:true }] },
    tests:[{ name:'Validation annotations on DTO', visible:true, validate:(c)=>({ passed:/@Valid/.test(c)&&/@Not(Blank|Null|Empty)/.test(c), message:'Use @Valid + validation constraints on DTO fields' }) }],
    hiddenTests:[], hints:['@NotBlank(message = "Name required") String name;','@Email String email; @Min(18) int age; @Size(min=8) String password;','In controller: public User create(@Valid @RequestBody CreateUserRequest req)'],
    solution:'record CreateUserRequest(\n    @NotBlank(message = "Name is required") String name,\n    @NotBlank @Email(message = "Valid email required") String email,\n    @Min(value = 18, message = "Must be 18+") int age,\n    @Size(min = 8, message = "Password min 8 chars") String password\n) {}\n\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    @PostMapping\n    public ResponseEntity<String> create(@Valid @RequestBody CreateUserRequest req) {\n        return ResponseEntity.status(201).body("Created: " + req.name());\n    }\n}',
    explanation:'Bean Validation catches bad data BEFORE it reaches your business logic. @NotBlank checks for null/empty strings, @Email validates format, @Min/@Max for numeric ranges, @Size for length. @Valid triggers validation — Spring returns 400 Bad Request with error details automatically. ALWAYS validate input in production.'
  },
  { id:'ra-006', worldId:'rest-api-dept', order:6, title:'@ExceptionHandler & @RestControllerAdvice', difficulty:'medium', xpReward:260, coinReward:55, estimatedTime:15,
    story:'When things go wrong, return clean error responses — not ugly stack traces.',
    objective:'Create a @RestControllerAdvice with @ExceptionHandler methods for custom exceptions. Return structured ErrorResponse with proper HTTP status.',
    starterCode:'// TODO: Custom exception classes\n// TODO: ErrorResponse record\n// TODO: @RestControllerAdvice with @ExceptionHandler methods',
    requirements:{ annotations:['RestControllerAdvice','ExceptionHandler'], patterns:[{ name:'ResponseEntity', pattern:'ResponseEntity', required:true },{ name:'HttpStatus', pattern:'HttpStatus|status', required:true }] },
    tests:[{ name:'Global exception handler', visible:true, validate:(c)=>({ passed:/@RestControllerAdvice/.test(c)&&/@ExceptionHandler/.test(c)&&/ResponseEntity/.test(c), message:'@RestControllerAdvice + @ExceptionHandler + ResponseEntity' }) }],
    hiddenTests:[], hints:['class ResourceNotFoundException extends RuntimeException { ... }','@ExceptionHandler(ResourceNotFoundException.class)','Return ResponseEntity.status(HttpStatus.NOT_FOUND).body(new ErrorResponse(...))'],
    solution:'class ResourceNotFoundException extends RuntimeException {\n    public ResourceNotFoundException(String msg) { super(msg); }\n}\nclass BadRequestException extends RuntimeException {\n    public BadRequestException(String msg) { super(msg); }\n}\nrecord ErrorResponse(int status, String message, long timestamp) {}\n\n@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(ResourceNotFoundException.class)\n    public ResponseEntity<ErrorResponse> handleNotFound(ResourceNotFoundException ex) {\n        return ResponseEntity.status(HttpStatus.NOT_FOUND)\n            .body(new ErrorResponse(404, ex.getMessage(), System.currentTimeMillis()));\n    }\n    @ExceptionHandler(BadRequestException.class)\n    public ResponseEntity<ErrorResponse> handleBadRequest(BadRequestException ex) {\n        return ResponseEntity.status(HttpStatus.BAD_REQUEST)\n            .body(new ErrorResponse(400, ex.getMessage(), System.currentTimeMillis()));\n    }\n}',
    explanation:'@RestControllerAdvice is a global interceptor for exceptions across ALL controllers. @ExceptionHandler targets specific exception types. Always return structured error responses with status code, message, and timestamp. Create specific exception classes for different error scenarios — never throw generic RuntimeException in production.'
  },
  { id:'ra-007', worldId:'rest-api-dept', order:7, title:'Service & Repository Layers', difficulty:'medium', xpReward:270, coinReward:55, estimatedTime:18,
    story:'Real APIs follow Controller → Service → Repository architecture. Each layer has a single responsibility.',
    objective:'Create @Service and @Repository classes. Use constructor injection (no @Autowired). Controller delegates to Service, Service delegates to Repository.',
    starterCode:'// TODO: @Repository class ProductRepository\n// TODO: @Service class ProductService (inject repository)\n// TODO: @RestController class ProductController (inject service)\n// Use constructor injection throughout',
    requirements:{ annotations:['Service','Repository','RestController'], patterns:[{ name:'constructor injection', pattern:'private\\s+final\\s+\\w+(Service|Repository)', required:true }] },
    tests:[{ name:'3-layer architecture', visible:true, validate:(c)=>({ passed:/@Service/.test(c)&&/@Repository/.test(c)&&/@RestController/.test(c)&&/private\s+final/.test(c), message:'All 3 layers with constructor injection' }) }],
    hiddenTests:[], hints:['@Repository has data access methods: save(), findById(), findAll(), deleteById()','@Service has business logic and injects repository via constructor.','@RestController injects service via constructor — no @Autowired needed!'],
    solution:'@Repository\nclass ProductRepository {\n    private final List<Product> db = new ArrayList<>();\n    public List<Product> findAll() { return db; }\n    public Optional<Product> findById(Long id) { return db.stream().filter(p->p.id().equals(id)).findFirst(); }\n    public Product save(Product p) { db.add(p); return p; }\n}\n\n@Service\nclass ProductService {\n    private final ProductRepository repository;\n    public ProductService(ProductRepository repository) { this.repository = repository; }\n    public List<Product> getAll() { return repository.findAll(); }\n    public Product create(Product p) { return repository.save(p); }\n}\n\n@RestController\n@RequestMapping("/api/products")\nclass ProductController {\n    private final ProductService service;\n    public ProductController(ProductService service) { this.service = service; }\n    @GetMapping\n    public List<Product> getAll() { return service.getAll(); }\n    @PostMapping\n    public Product create(@RequestBody Product p) { return service.create(p); }\n}',
    explanation:'Controller → Service → Repository is THE standard Spring architecture. Controller handles HTTP, Service has business logic, Repository does data access. Constructor injection makes dependencies explicit and immutable (use private final). Spring auto-injects when there is a single constructor — no @Autowired needed. This separation enables testing, maintainability, and team collaboration.'
  },
  { id:'ra-008', worldId:'rest-api-dept', order:8, title:'DTO Pattern & ModelMapper', difficulty:'medium', xpReward:260, coinReward:55, estimatedTime:15,
    story:'Never expose your Entity directly! Use DTOs (Data Transfer Objects) to control what the API returns.',
    objective:'Create separate Request DTO and Response DTO. Map between Entity and DTOs in the Service layer.',
    starterCode:'// TODO: Entity: Product(Long id, String name, double price, String internalCode)\n// TODO: Request DTO: CreateProductRequest(String name, double price)\n// TODO: Response DTO: ProductResponse(Long id, String name, double price)\n// Map in service layer — never expose internalCode!',
    requirements:{ patterns:[{ name:'Request DTO', pattern:'Request|Dto|DTO', required:true },{ name:'Response DTO', pattern:'Response|Dto|DTO', required:true },{ name:'Mapping logic', pattern:'new\\s+\\w+(Response|DTO)|toResponse|toDto|mapper', required:true }] },
    tests:[{ name:'Separate Request and Response DTOs', visible:true, validate:(c)=>({ passed:/(Request|RequestDto|CreateProduct)/.test(c)&&/(Response|ResponseDto|ProductResponse)/.test(c), message:'Create both Request and Response DTOs' }) }],
    hiddenTests:[], hints:['Request DTO has only the fields clients can send.','Response DTO has only the fields clients should see.','Map in service: new ProductResponse(entity.id(), entity.name(), entity.price())'],
    solution:'record Product(Long id, String name, double price, String internalCode) {} // Entity\nrecord CreateProductRequest(String name, double price) {} // Request DTO\nrecord ProductResponse(Long id, String name, double price) {} // Response DTO\n\n@Service\nclass ProductService {\n    public ProductResponse create(CreateProductRequest req) {\n        Product entity = new Product(1L, req.name(), req.price(), "INTERNAL-001");\n        return toResponse(entity);\n    }\n    private ProductResponse toResponse(Product p) {\n        return new ProductResponse(p.id(), p.name(), p.price());\n    }\n}',
    explanation:'DTOs decouple your API contract from your database model. Request DTOs validate input. Response DTOs control output — hiding sensitive fields like passwords, internal codes, or audit columns. This is mandatory in production APIs. Use Java records for concise DTOs.'
  },
  { id:'ra-009', worldId:'rest-api-dept', order:9, title:'Pagination & Sorting', difficulty:'hard', xpReward:300, coinReward:65, estimatedTime:18,
    story:'Real APIs with thousands of records need pagination. No one fetches ALL products at once.',
    objective:'Implement pagination using Pageable, @RequestParam for page/size/sort. Return a Page<T> response.',
    starterCode:'import org.springframework.data.domain.*;\n\n// TODO: GET /api/products?page=0&size=10&sort=name,asc\n// Accept Pageable parameter\n// Return Page<ProductResponse>',
    requirements:{ patterns:[{ name:'Pageable', pattern:'Pageable', required:true },{ name:'Page<', pattern:'Page<', required:true },{ name:'PageRequest or Sort', pattern:'PageRequest|Sort|Pageable', required:true }] },
    tests:[{ name:'Pagination with Pageable', visible:true, validate:(c)=>({ passed:/Pageable/.test(c)&&/Page</.test(c), message:'Use Pageable parameter and return Page<T>' }) }],
    hiddenTests:[], hints:['Spring auto-creates Pageable from ?page=0&size=10&sort=name,asc','@GetMapping public Page<Product> getAll(Pageable pageable)','Repository: findAll(pageable) returns Page<Product>.'],
    solution:'@RestController\n@RequestMapping("/api/products")\npublic class ProductController {\n    private final ProductRepository repository;\n\n    @GetMapping\n    public Page<ProductResponse> getAll(Pageable pageable) {\n        return repository.findAll(pageable)\n            .map(p -> new ProductResponse(p.getId(), p.getName(), p.getPrice()));\n    }\n\n    @GetMapping("/search")\n    public Page<ProductResponse> search(\n        @RequestParam String keyword,\n        @RequestParam(defaultValue = "0") int page,\n        @RequestParam(defaultValue = "10") int size,\n        @RequestParam(defaultValue = "name,asc") String sort\n    ) {\n        Pageable pageable = PageRequest.of(page, size, Sort.by(sort.split(",")[0]));\n        return repository.findByNameContaining(keyword, pageable)\n            .map(p -> new ProductResponse(p.getId(), p.getName(), p.getPrice()));\n    }\n}',
    explanation:'Pageable auto-binds page/size/sort from query params. Page<T> includes content, totalElements, totalPages, and metadata. Always paginate list endpoints in production — unbounded queries can crash your DB. Combine with .map() to convert entities to DTOs before returning.'
  },
  { id:'ra-010', worldId:'rest-api-dept', order:10, title:'🏆 Boss: Complete CRUD REST API', difficulty:'boss', xpReward:450, coinReward:120, estimatedTime:25,
    story:'⚔️ BOSS CHALLENGE! Build a production-quality REST API with every pattern: Controller→Service→Repository, DTOs, validation, error handling, pagination.',
    objective:'Build Employee API: POST(201), GET all with pagination(200), GET by id(200/404), PUT(200/404), DELETE(204/404). Include @Valid, @RestControllerAdvice, DTOs, ResponseEntity.',
    starterCode:'// Build COMPLETE Employee REST API\n// 1. Entity + Request DTO + Response DTO\n// 2. @Repository\n// 3. @Service with DTO mapping\n// 4. @RestController with all CRUD + pagination\n// 5. @RestControllerAdvice error handling\n// 6. @Valid on POST/PUT',
    requirements:{ annotations:['RestController','Service','Repository','Valid','RestControllerAdvice','ExceptionHandler','PostMapping','GetMapping','PutMapping','DeleteMapping','RequestBody','PathVariable'] },
    tests:[{ name:'All CRUD + validation + error handling', visible:true, validate:(c)=>{ const a=['@RestController','@Service','@Repository','@Valid','@RestControllerAdvice','@PostMapping','@GetMapping','@PutMapping','@DeleteMapping']; const f=a.filter(x=>c.includes(x)); return { passed:f.length>=8, message:`Found ${f.length}/9 required annotations` }; } }],
    hiddenTests:[], hints:['Start with Entity, DTOs, then Repository.','Service maps between Entity and DTOs.','Controller uses ResponseEntity for proper status codes.'],
    solution:'// See explanation for complete architecture pattern.',
    explanation:'A production CRUD API has: Entity with JPA annotations, Request/Response DTOs with validation, Repository extending JpaRepository, Service with business logic and DTO mapping, Controller with proper HTTP methods and ResponseEntity, GlobalExceptionHandler with custom exceptions. This is the EXACT pattern used in every professional Spring Boot project. You now know how to build real backend APIs.'
  }
];
